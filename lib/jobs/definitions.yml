- name: consolidate-record
  concurrency: 5
  uniqueKey: recordId
  timeout: 30000
  options:
    attempts: 2
    backoff:
      delay: 10000
      type: fixed

- name: elect-record
  concurrency: 50
  uniqueKey: recordId
  timeout: 5000
  options:
    attemps: 5
    backoff:
      delay: 10000
      type: fixed

- name: incoming-webhook-link-analyzer
  concurrency: 10

- name: harvest-csw
  concurrency: 2

- name: lookup-wfs
  concurrency: 5
